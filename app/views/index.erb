<h1>Welcome to the home page</h1>


<div class="container">
  <div class="row">
    <div class="col-sm-4">
    	<% @wishes.each do |wish| %>
    		<a href="/wishes/<%= wish.id%>"><%= wish.title%></a>
    	<% end %>
    </div>
  </div>
  
</div>